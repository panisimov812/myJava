6️⃣ Очень короткое резюме
Generic-методы
<T> T method(T arg) — тип определяется при вызове.

Generic-классы
class Box<T> — тип определяется при создании объекта.

Ограничения
<T extends Number> — T = Number или его потомки.

