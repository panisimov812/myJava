package itmoLessons.lesson15Map;

import java.util.HashMap;

/**
 * 🔹 Что такое Map в Java
 *
 * Map<K, V> — это структура данных, которая хранит пары ключ–значение.
 *
 * Главное:
 *  🔑 Ключи уникальны
 *  📦 Значения могут повторяться
 *  Один ключ → одно значение
 *  Самая популярная реализация — HashMap.
 *
 * 🔹 Как работает HashMap
 * Внутри:
 *  Берётся hashCode() ключа
 *  Вычисляется индекс в массиве (бакет)
 *  Если в бакете уже есть элементы — используется:
 * LinkedList (до Java 8)
 *  Tree (если элементов > 8)
 * ⚠ Поэтому для собственных объектов обязательно переопределять equals() и hashCode()
 */
public class GptLesson {
    public static void main(String[] args) {
        HashMap<String, Integer> hashMap = new HashMap<>();
        //put()
        //  если ключа нет то добавляет
        //  если ключ есть значение
        hashMap.put("a", 1);
        hashMap.put("Москва", 790);
        hashMap.put("Ростов", 270);
        hashMap.put("Великий Новгород", 200);

        //ВАЖНО!
        //  Разрешает один null ключ
        //  Разрешает несколько null значений
        hashMap.put(null, null);

        //Удаление по ключу — удаляет пару полностью
        //Это удаление:
        //Удалит только если совпадают и ключ и значение
        //У тебя значение 790 → не удалится ✔
        hashMap.remove(null);
        hashMap.remove("Москва", 5);

        //Здесь удалится, потому что совпадает и ключ, и значение ✔
        //💡 Это безопасный способ удаления — если боишься, что значение уже изменилось.
        hashMap.remove("Ростов", 270);


        //Замена
        //Заменит если ключ существует.
        hashMap.replace("Великий Новгород", 100);

        //Заменит только если:
        //ключ есть
        //значение равно 12

        //Это атомарная проверка — удобно при многопоточности.
        hashMap.replace("Тверь", 12, 23);

        System.out.println(hashMap.get("a"));
    }
}
