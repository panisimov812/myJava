package itmoLessons.lesson2;

public class SecondLesson {
    public static void main(String[] args) {
        //логические операторы
        int x = 50, y = 100, z = 91;
        //&& оператор и
        boolean res = (x % 2 == 0) && (y % 2 == 0) && (z % 2 == 0);
        System.out.println();
    }
}
